{
  "name": "smart-news-hub", 
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "smart-news-frontend",
    "smart-news-backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=smart-news-frontend & npm run start --workspace=smart-news-backend",
    "build": "npm run build --workspace=smart-news-frontend && npm run build --workspace=smart-news-backend",
    "start": "npm run start --workspace=smart-news-backend",
    "test": "echo \"No test specified\""
  },
  "devDependencies": {
    "lerna": "^7.x.x" 
  }
}